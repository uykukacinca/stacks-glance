{
  "scripts": {
    "format": "pnpm exec prettier . --write",
    "log": "docker logs -f glance-test"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
