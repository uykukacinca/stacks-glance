- name: "Containers"
  slug: containers
  width: wide
  # hide-desktop-navigation: true
  center-vertically: true
  columns:
    - size: full
      widgets:

        - <<: *containers
          title: core
          category: core

        - <<: *containers
          title: media
          category: media

        - <<: *containers
          title: apps
          category: apps

        - <<: *containers
          title: arr-suite
          category: arr-suite

        - <<: *containers
          title: dev
          category: dev

    - size: full
      widgets:

        - <<: *docker-containers

