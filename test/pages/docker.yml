- name: "Docker"
  slug: docker
  # width: wide
  # hide-desktop-navigation: true
  center-vertically: true
  columns:
    - size: full
      widgets:
        - type: docker-containers
          css-class: widget-containers-custom icon-only light-up
          running-only: true

        - type: docker-containers
          css-class: widget-containers-custom hide-running
          hide-by-default: false
