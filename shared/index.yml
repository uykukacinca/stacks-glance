document:
  head: |
    <script>
      const GLIMPSE_URL = "${GLIMPSE_URL}";
      const HOST_NAME = "${HOST_NAME}";
      const ICONS_URL = "${ICONS_URL}";
      const NTFY_URL = "${NTFY_URL}";
      const BACKUBE_URL = "${BACKUBE_URL}";
    </script>
    <script src="/assets/js/hls.js"></script>
    <script src="/assets/js/custom.js"></script>
    <script src="/assets/js/xeddit.js"></script>
    <link rel="stylesheet" href="/assets/css/main.css">
    <script src="${UMAMI_URL}/script.js" data-website-id="${UMAMI_ID}"></script>

$include: theme.yml

shared:
  - $include: variables.yml
  - $include: definitions.yml
  - $include: /widgets/index.yml
  - $include: /widgets/wip/index.yml
